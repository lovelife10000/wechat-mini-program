<view >
  <block wx:for="{{func}}">
    <navigator url="{{item.url}}">
      <view class="name">{{item.name}}</view>
    
        <image class="name-right" src="../../images/ico_right.png"></image>
   
    </navigator>

  </block>

</view>